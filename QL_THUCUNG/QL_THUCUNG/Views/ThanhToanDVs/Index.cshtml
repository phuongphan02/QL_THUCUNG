@model IEnumerable<QL_THUCUNG.Models.ThanhToanDV>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_ThanhVien)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_ThanhVien)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_TTDV }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_TTDV }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_TTDV })
        </td>
    </tr>
}

</table>
